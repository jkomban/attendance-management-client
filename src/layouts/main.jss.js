const style = theme => ({
    root: {
        display: 'flex',
        flexDirection: 'column'
    },
    offset: theme.mixins.toolbar,
    contentContainer: {
        display: 'flex',
    }
})

export default style;