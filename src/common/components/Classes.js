import React from 'react';
import { Button } from '@material-ui/core';

const Classes = () => (
    <div>
        <h1>This is Classes</h1>
        <Button variant="contained" color="primary">classes</Button>
    </div>
);

export default Classes;
